import { createTheme } from '@mui/material/styles';;

const myTheme = createTheme({
  typography: {
    fontFamily: 'Kanit'
  },
  palette: {
    secondary: {
      main: '#00B25A',
    },
  },
});

export default myTheme;
